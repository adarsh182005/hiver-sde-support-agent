# Baselines

The final evaluation will compare the AI support agent against at least:

1. Majority-class baseline.
2. TF-IDF + linear classifier / lexical retrieval baseline.

Baseline implementations will be added after the dataset schema and intent taxonomy are fixed.